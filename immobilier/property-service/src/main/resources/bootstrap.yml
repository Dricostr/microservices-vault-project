spring:
  application:
    name: property-service
  cloud:
    vault:
      uri: http://4.233.210.227:8200
      authentication: APPROLE
      app-role:
        role-id: ${SPRING_CLOUD_VAULT_APP_ROLE_ROLE_ID}
        secret-id: ${SPRING_CLOUD_VAULT_APP_ROLE_SECRET_ID}
      kv:
        enabled: true
        backend: secret
        default-context: immobilier/property-service
      generic:
        enabled: false